spring:
  application:
    name: microService

server:  
  port: 8083   # HTTP port

# Discovery Server Access
eureka:  
  client:
    registerWithEureka: true
    fetchRegistry: true	
    serviceUrl:
      defaultZone: http://discovery-server:1111/eureka/