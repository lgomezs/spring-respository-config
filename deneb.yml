server:  
  port: ${PORT:2222}

eureka:  
  client:
    serviceUrl:
      defaultZone: http://discovery-server:1111/eureka/

microServiceRest:  
  microService:
    name: contacto
    pingUrl: http://${microServiceRest.microService.name}/